import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'spy',
  webDir: 'www',
  // server: {
  //   // GitHub Pages'de gerekli olan base url yapılandırması
  //   url: '/spy/', // Repository adını buraya yaz (eğer repository adı farklıysa güncelle)
  //   cleartext: true // Geliştirme sırasında sorun yaşamamak için
  // }
};

export default config;
